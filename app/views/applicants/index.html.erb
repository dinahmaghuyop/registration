<h1>Listing applicants</h1>

<table>
  <tr>
    <th>Last name</th>
    <th>First name</th>
    <th>Course</th>
    <th>Grad date</th>
    <th>Contact number</th>
    <th>App type</th>
    <th>Email</th>
    <th>Start date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @applicants.each do |applicant| %>
  <tr>
    <td><%= applicant.last_name %></td>
    <td><%= applicant.first_name %></td>
    <td><%= applicant.course %></td>
    <td><%= applicant.grad_date %></td>
    <td><%= applicant.contact_number %></td>
    <td><%= applicant.app_type %></td>
    <td><%= applicant.email %></td>
    <td><%= applicant.start_date %></td>
    <td><%= link_to 'Show', applicant %></td>
    <td><%= link_to 'Edit', edit_applicant_path(applicant) %></td>
    <td><%= link_to 'Destroy', applicant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Applicant', new_applicant_path, :class => "btn" %>
